# AI_USAGE.md

I used Claude (claude-sonnet-4-6 via Claude Code CLI) as the primary coding assistant throughout this assignment. Claude helped scaffold the project structure (server directory layout, tsconfig, Express + SQLite wiring), generate component boilerplate (UserRow, FilterInput, Skeleton), and draft CSS modules. I reviewed and adjusted all generated code — in particular the BiDi/RTL implementation on Screen 3, the Zustand store shape, and the SQLite schema — before committing. Every architectural decision in DECISIONS.md reflects my own judgment: I directed the choices (Zustand over TanStack Query, SQLite over JSON file, single debounced filter input, skeleton extension), and I can defend every line. "Claude wrote it" is not an explanation I'd accept from a team member, and it's not one I'm offering here.
