# AI_USAGE.md

I used Claude (claude-sonnet-4-6 via Claude Code CLI) as the primary coding assistant throughout this assignment.

Claude helped scaffold the project structure (server directory layout, tsconfig, Express), generate component boilerplate (UserRow, FilterInput, Skeleton), and draft CSS modules.

I directed the choices (Zustand over TanStack Query, SQLite over JSON file, single debounced filter input, skeleton extension).

*I love TanStack and use it almost always, for this project there was no need.
